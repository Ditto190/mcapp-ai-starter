# PostgreSQL connection — from Docker stack (docker-compose.yml at foam-modme root)
# Internal Docker hostname: postgres, port: 5432
# For local dev: expose port 5432 in docker-compose.yml or use a local PostgreSQL instance
DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:5432/${POSTGRES_DB}"
