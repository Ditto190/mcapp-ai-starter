---
stage: 1
task: 9
title: "PII rules template — docs/knowledge-platform/pii-rules-guide.md"
agent: gem-implementer
status: Success
important_findings: false
compatibility_issues: false
---

## Summary

Created the PII rules documentation guide covering the `.pii-rules.csv` format, security constraints, and agent access restrictions.

## Details

Created `docs/knowledge-platform/pii-rules-guide.md`. The guide documents:

- File format: CSV with columns pattern, replacement, type
- Security constraints: `chmod 600`, never committed, never read by agents
- Example patterns (without real PII): client names, email formats, project codes
- Phase 2+ pipeline: how the blocking script reads the file and applies replacements
- Agent access restriction: all agents must NOT access `.pii-rules.csv` regardless of path provided

`.gitignore` updated to include `.pii-rules.csv` entry. The actual rules file is NEVER created by agents — user creates it manually with their own PII patterns.

## Output

- `docs/knowledge-platform/pii-rules-guide.md`
- `.gitignore` — `.pii-rules.csv` entry added

## Validation

Guide file exists with all sections. `.gitignore` contains `.pii-rules.csv` line.

## Issues

None.
